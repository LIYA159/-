{
            Double a;
            Double x = Convert.ToInt32(Console.ReadLine());


            {
                a = Math.Sin(Math.PI/x);
                Console.WriteLine(a);
                Console.ReadKey();
            }
        }
    }
}

